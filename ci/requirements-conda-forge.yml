name: test_env
channels:
  - conda-forge
dependencies:
  # python=3.7.1=h5001a0f_0 is broken
  - python=3.7.1=h0371630_3
  - dask
  - flake8
  - gcc_linux-64
  - numba
  - numpy
  - pandas
  - pytest
  - scipy
  - xarray
  # Only needed by test_ncdiff
  - h5netcdf
  - pip:
    - coveralls
    - pytest-cov
