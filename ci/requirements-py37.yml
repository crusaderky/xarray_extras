name: test_env
channels:
  - anaconda
dependencies:
  - python=3.7
  - dask
  - gcc_linux-64
  - numba
  - pytest
  - pytest-cov
  - scipy
  - xarray
  # code linters (running only in the latest version of Python)
  - flake8
  - mypy
